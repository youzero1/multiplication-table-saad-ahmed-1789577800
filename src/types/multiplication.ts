export type MultiplicationRow = {
  /** The number whose table this is, e.g. 2. */
  multiplicand: number;
  /** The factor it is multiplied by, e.g. 3. */
  multiplier: number;
  /** multiplicand × multiplier */
  product: number;
};
